#!/bin/bash

# Check if the file exists
if [ -e "hello" ]; then
    # Change the owner to "betty"
    sudo chown betty hello
    echo "Owner of 'hello' file has been changed to 'betty'."
else
    echo "The 'hello' file does not exist."
fi
